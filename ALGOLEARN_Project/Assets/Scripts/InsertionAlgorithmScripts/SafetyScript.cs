using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SafetyScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
/*
 
 
 void CurrentObjectBeingSorted()
    {
        if(temp.GetComponent<ObjectScript>().ObjectIsSelected_1 == true)
        {
            temp.gameObject.GetComponent<MeshRenderer>().material = mat_1;
        }
    }
    void SelectObjects()
    {
        for (int i = 0; i < 5; i++)
        {
            if(spawnList[i].gameObject.GetComponent<ObjectScript>().CurrentObjectSelected == true && spawnList[i].transform.position.x != temp.transform.position.x)
            {
                temp2 = spawnList[i];
                temp2.gameObject.GetComponent<MeshRenderer>().material = mat;
            }
        }
    }
    void checkIfObjectsAreNextToEachOther()
    {
        for (int i = 0; i < 5; i++)
        {
            if (((int)temp.gameObject.transform.position.x + 2 == (int)spawnList[i].transform.position.x))
            {
                temp2 = spawnList[i];
                temp2.gameObject.GetComponent<MeshRenderer>().material = mat;
            }
        }
    }

 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 */
